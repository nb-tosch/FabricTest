core:
  # For initial customer deployment, map both default and main.
  workspace_id:
    default: "00000000-0000-0000-0000-000000000000"
    main: "00000000-0000-0000-0000-000000000000"

  # Deploy from repository root
  repository_directory: "."

  # Keep scope explicit for first rollout
  item_types_in_scope:
    - Notebook
    - DataPipeline
    - Lakehouse
    - Warehouse

  # Explicitly enable parameterization
  parameter: "parameter.yml"

publish:
  # Run publish during initial deployment
  skip:
    default: false
    main: false

unpublish:
  # For initial project bootstrap, do NOT remove orphans automatically
  skip:
    default: true
    main: true
